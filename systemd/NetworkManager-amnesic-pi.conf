# Drop-in for NetworkManager.service, equivalent to the systemd-networkd one.
# Installed unconditionally so that enabling NetworkManager later cannot
# quietly produce a network manager that is not gated on the firewall.
[Unit]
BindsTo=amnesic-pi-firewall.service
After=amnesic-pi-firewall.service
